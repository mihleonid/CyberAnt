#include "configurator.h"

static void Configurator::configureResourceNode(ResourceType t, int& add, int& addP, int& taxMin, int& taxMax){
	add=-1;
	addP=10;
	taxMin=0;
	taxMax=20;
	//TODO from files
}

